# Dossier Public

Placez le logo BZ dans ce dossier avec le nom `logo-bz.png`.

Le logo sera automatiquement affiché dans l'en-tête de l'application.

Formats supportés : PNG, JPG, SVG
Dimensions recommandées : Largeur max 200px, Hauteur max 60px

